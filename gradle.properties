android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -XX:+HeapDumpOnOutOfMemoryError
sdkVersion=1.0.36

# SPEC-070-A J.25 — library-friendly Gradle flags.
# nonTransitiveRClass: each module gets its own R, prevents bloat for hosts.
# enableJetifier=false: we only depend on AndroidX, so the support-lib rewrite
# tool can stay off and shave a few seconds off every build.
# kotlin.code.style=official: pin formatter to Kotlin official style guide.
android.nonTransitiveRClass=true
android.enableJetifier=false
kotlin.code.style=official

# AppDNA cross-platform feature-parity marker (SPEC-070-0).
# Android SDK 1.0.36 wraps iOS 1.0.64: SPEC-404 hard SDK suspension. Adds
# per-key `sdk_api_keys.suspended_at` mirror, AppDNA.runtimeLock state +
# AppDNALifecycleDelegate (codegen'd), paywall_trigger / presentPaywall /
# message / survey gates against runtime_lock. Bootstrap response now carries
# `runtime_lock` from the backend when the tenant has been swept (day 20+)
# or org cancelled. CI (`pnpm check:version-lockstep`) enforces parity
# equality with iOS s.version.
appdnaFeatureParity=1.0.64
