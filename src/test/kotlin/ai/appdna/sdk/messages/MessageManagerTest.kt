package ai.appdna.sdk.messages

import org.junit.Test

/**
 * The pre-Wave-1 MessageManagerTest tested an inline MessageFrequencyTracker
 * that no longer exists (Wave 1 F.12 extracted it to its own file with a
 * different constructor signature). Coverage of the new tracker lives in
 * `InAppMessagingTest.kt`. This stub remains only so the previously-synced
 * file on the Mac bridge still compiles after sync.
 */
class MessageManagerTest {
    @Test fun `obsolete test stub`() = Unit
}
